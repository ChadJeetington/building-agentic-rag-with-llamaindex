Can think of an index as a set of metadata over our data. 

Vector index = indexes nodes via text embeddings. The abstraction for any kind of RAG system.
- Querying a vector index returns the most similiar nodes via embedding similiarity. 